# CLAUDE.md

## 작업 규칙

> 출처: Andrej Karpathy 가 2026-01-26 X 에 올린 "LLM 코딩의 실패 유형" 지적에서
> 파생된 규칙입니다. 파일 자체는 카파시가 쓴 것이 아니라 Forrest Chang 이 만든
> `andrej-karpathy-skills` 저장소의 내용을, 이 프로젝트에 맞게 옮겨 적었습니다.

### 1. 코딩 전에 생각하기

**추측하지 마라. 혼란을 감추지 마라. 트레이드오프를 드러내라.**

- 가정을 세웠으면 코드에 묻지 말고 말로 꺼낼 것
- 요청이 두 가지로 읽히면 양쪽을 제시하고, 답에 따라 작업이 크게 갈릴 때만 물어볼 것
- 더 단순한 방법이 보이면 시키는 대로 하기 전에 먼저 제안할 것
- 모르는 것은 모른다고 할 것 — 특히 외부 사실, 라이브러리 동작, 버전별 차이

### 2. 단순한 것 먼저

**문제를 푸는 최소한의 코드. 추측성은 없이.**

- 요청하지 않은 기능을 얹지 말 것
- 한 번만 쓰이는 추상화를 만들지 말 것
- 일어나지 않을 오류를 위한 처리를 넣지 말 것
- 특수 케이스를 덧대기 전에, 밑단을 일반화하는 편이 나은지 먼저 볼 것

### 3. 수술하듯 최소한만 건드리기

**꼭 필요한 것만. 내가 어지른 것만 치운다.**

- 주변 코드의 스타일·주석 밀도·명명을 그대로 따를 것
- 무관한 리팩터를 끼워 넣지 말 것 — 개선점이 보이면 **말로 보고하고 승인받을 것**
- 내 변경 때문에 쓰이지 않게 된 import·변수만 지울 것
- 요청 범위를 임의로 넓히지도, 좁히지도 말 것

### 4. 목표 기반 실행

**성공 기준을 정하고, 검증될 때까지 반복한다.**

- 작업을 "무엇이 확인되면 끝인가"로 바꿔 놓고 시작할 것
- 다단계 작업은 중간 검증 지점을 두고 진행할 것
- 됐다고 말하기 전에 실제로 돌려볼 것. 못 돌려봤으면 **못 돌려봤다고 명시할 것**
- 테스트가 실패하면 출력을 그대로 보여줄 것

---

## 이 프로젝트에서

### 구조

앱은 얇은 클라이언트입니다. **판단은 전부 서버에 있습니다.**

    [앱] 사진 → 업로드 → [서버] 전처리 → OCR → 발행처 식별 → 파싱 → 검산 → JSON

- `ocr.py` Google Vision (인증키는 서버에만 — APK 에 절대 넣지 말 것)
- `issuer.py` 발행처 식별 + 새 양식 자동 학습 (`profiles.db`)
- `parser.py` / `core.py` / `parsers/` 양식별 파싱
- `receipt_db.py` 앱이 올린 인식 내역 (`receipts.db`)
- `csv_export.py` CSV 형식 — PC CLI 와 서버가 공유. 앱(`Export.kt`)은 오프라인
  내보내기가 필요해 별도 구현이므로, 열 구성을 바꾸면 **양쪽 다** 고칠 것

**파서를 고치면 앱 재설치 없이 반영됩니다.** 앱을 다시 빌드하기 전에 서버만
고쳐도 되는 문제인지 먼저 확인할 것.

### 도구 체인 (매번 걸리는 것들)

| 상황 | 반드시 이렇게 |
|---|---|
| 파이썬 실행 | `python` 아니라 **`py`** — `python` 은 Microsoft Store 스텁이라 exit 49 |
| Gradle 빌드 | 먼저 `$env:JAVA_HOME = "D:\Android\Android Studio\jbr"` — 기본 `java` 는 Java 8 |
| APK 위치 | `C:\AndroidBuild\...` — OneDrive 가 빌드 파일을 잠가 산출물을 밖으로 뺐음 |
| `.cmd`/`.bat` 작성 | **CP949(ANSI) + CRLF**. UTF-8 이나 LF 로 저장하면 cmd.exe 가 깨뜨림 |

### 파서를 고칠 때

정규식 하나가 여러 곳에서 쓰이므로, 눈으로 확인하지 말고 **샘플 전체를 돌려
전후를 비교할 것.** OCR 결과를 캐시해 두면 Vision 을 다시 호출하지 않습니다.

성공 기준: `Sample/` 전체의 품목·금액·경고가 의도한 파일 외에는 **완전히 동일**.

### 비밀

`api_key.txt`(앱↔서버 인증)와 Vision 서비스 계정 JSON 은 커밋 금지 —
`.gitignore` 에 등록돼 있습니다. 대화나 문서에 키를 적어야 할 때는
사용자 본인 것임을 확인하고 적을 것.
